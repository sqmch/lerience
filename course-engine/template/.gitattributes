# Normalize all text files to LF in the repo and on checkout, regardless of a
# contributor's core.autocrlf. Without this, a fresh Windows checkout would get
# CRLF working files and `npm run format:check` would fail spuriously while CI
# (Linux, LF) stays green. Binary files are auto-detected and left untouched.
* text=auto eol=lf
