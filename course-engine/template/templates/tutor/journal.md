# Tutor journal

Append-only session log. Newest entries at the bottom. The tutor reads the last 2–3 entries
at every session open — this file is the tutor's cross-session memory; the repo remembers so
the model doesn't have to.

---
