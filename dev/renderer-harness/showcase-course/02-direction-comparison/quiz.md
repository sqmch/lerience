# Recall — comparing directions

1. In one sentence, what does the dot product measure?
2. `dot([3, 4], [6, 8])` is 50 and `dot([3, 4], [3, 4])` is 25, yet the pairs point the same way.
   What is the dot product missing, and how does cosine similarity fix it?
3. What cosine value represents a right angle? Opposite directions?
4. Why does cosine similarity divide by the product of both magnitudes rather than by one of them?
5. Why is cosine similarity undefined for a zero vector, and what should a function do about it?
6. A ranking by raw dot product favours long vectors. Give one concrete example from the chapter
   where that gives the wrong answer.
