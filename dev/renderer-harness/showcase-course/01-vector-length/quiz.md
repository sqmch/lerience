# Recall — vector length

1. `[3, 4].length` is 2. What is the magnitude of `[3, 4]`, and why are those different questions?
2. Where does the square root in the magnitude formula come from?
3. Why does `magnitude([-3, 4])` equal `magnitude([3, 4])`?
4. What is a unit vector, and what information does it keep from the original?
5. Why is `normalize([0, 0])` a problem, and what are two acceptable ways to handle it?
6. The magnitude loop never mentions "2". What does that tell you about vectors with 384
   components?
